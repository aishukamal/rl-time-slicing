# POC2 — TIMESLICE job A (Math RLVR, lr=1e-6). Identical workload to POC1 job A.
# Derived from POC1 manifests/job-timeslice-a.yaml (async-longcot math trainer).
# Template variables (render with: envsubst '${NODE_NAME} ${RUN_SECONDS}'):
#   ${NODE_NAME}    the single h100-mega-8gpu-spot-b node all PoC pods share
#   ${RUN_SECONDS}  training budget passed to `timeout`
#
# Topology (node GPU indexes): 0 = job-a rollout (vLLM, dedicated),
# 1 = job-b rollout, 2 = SHARED trainer GPU (time-sliced A+B).
# GPU mask "2,0": shared GPU FIRST — verl creates the trainer placement group
# before the rollout one and Ray hands out visible ordinals in mask order, so
# the trainer lands on node GPU 2 and vLLM on node GPU 0 (findings Q7). The
# in-script orientation watchdog enforces this (dies loudly on violation).
#
# No nvidia.com/gpu resources: the pod is privileged and gets GPU access via
# the same hostPath driver/device mounts the snapshot-agent DaemonSet uses
# (GKE COS: /home/kubernetes/bin/nvidia + /dev). NVIDIA_VISIBLE_DEVICES is NOT
# honored without the NVIDIA container runtime (GKE uses the device plugin),
# so the script derives CUDA_VISIBLE_DEVICES from GPU_MASK at runtime.
# hostPID + SYS_PTRACE + seccomp Unconfined: required for verl fully-async
# weight sync on GKE (pidfd_getfd is blocked by the default seccomp profile).
apiVersion: v1
kind: ConfigMap
metadata:
  name: poc2-job-a
  namespace: default
data:
  gpu_monitor.sh: |
    #!/usr/bin/env bash
    set -euo pipefail
    OUTPUT="${1:?Usage: $0 <output.csv> [interval_ms]}"
    INTERVAL_MS="${2:-100}"
    echo "timestamp_ms,gpu_index,gpu_util_pct,mem_util_pct,power_w" > "$OUTPUT"
    trap 'exit 0' SIGINT SIGTERM
    SLEEP_S=$(python3 -c "print($INTERVAL_MS/1000)")
    while true; do
        ts=$(date +%s%3N)
        nvidia-smi --query-gpu=index,utilization.gpu,utilization.memory,power.draw \
                   --format=csv,noheader,nounits 2>/dev/null | \
        while IFS=', ' read -r idx gpu mem pwr; do
            echo "${ts},${idx},${gpu},${mem},${pwr}"
        done >> "$OUTPUT"
        sleep "$SLEEP_S"
    done

  data_prep.py: |
    #!/usr/bin/env python3
    """Prepare DAPO-Math-17k subsets for the async long-CoT experiment.

    Source: HF dataset BytedTsinghua-SIA/DAPO-Math-17k (data/dapo-math-17k.parquet),
    which is ALREADY in verl RLHFDataset format:
      - data_source = "math_dapo"  -> default reward routing hits
        verl.utils.reward_score.math_dapo.compute_score (Answer: <ans> extraction,
        matching the instruction baked into every prompt)
      - prompt = [{"role": "user", "content": "...Remember to put your answer on its
        own line after \"Answer:\"..."}]
      - reward_model = {"style": "rule", "ground_truth": "<answer string>"}

    We only subsample (512 train / 64 val, disjoint) and sanity-check the schema.
    No agent_name column -> single_turn_agent (default), single-turn long CoT.
    """

    import argparse
    import os

    import pandas as pd
    from huggingface_hub import hf_hub_download


    def main():
        ap = argparse.ArgumentParser()
        ap.add_argument("--repo_id", default="BytedTsinghua-SIA/DAPO-Math-17k")
        ap.add_argument("--filename", default="data/dapo-math-17k.parquet")
        ap.add_argument("--out_dir", default="/workspace/data/dapo_math")
        ap.add_argument("--train_size", type=int, default=512)
        ap.add_argument("--val_size", type=int, default=64)
        ap.add_argument("--seed", type=int, default=17)
        args = ap.parse_args()

        local = hf_hub_download(repo_id=args.repo_id, filename=args.filename, repo_type="dataset")
        df = pd.read_parquet(local)
        print(f"loaded {len(df)} rows, columns: {list(df.columns)}")

        # ---- schema sanity checks (fail fast, before burning GPU time) ----
        row = df.iloc[0]
        prompt = row["prompt"]
        assert row["data_source"] == "math_dapo", f"unexpected data_source {row['data_source']!r}"
        p0 = prompt[0] if not isinstance(prompt, dict) else prompt
        p0 = dict(p0)
        assert p0.get("role") == "user" and isinstance(p0.get("content"), str), f"bad prompt row: {p0}"
        assert "Answer:" in p0["content"], "prompt lacks the Answer: instruction the reward fn expects"
        rm = dict(row["reward_model"])
        gt = rm.get("ground_truth")
        assert isinstance(gt, str) and gt, f"bad ground_truth: {gt!r}"
        print("schema OK; sample prompt (first 400 chars):")
        print(p0["content"][:400])
        print("sample ground_truth:", gt[:100])

        # Deduplicate on prompt text so train/val are truly distinct problems.
        df = df.assign(_ptext=df["prompt"].map(lambda p: dict(list(p)[0])["content"]))
        df = df.drop_duplicates("_ptext").drop(columns="_ptext").reset_index(drop=True)
        print(f"{len(df)} unique problems after dedup")

        n = args.train_size + args.val_size
        sub = df.sample(n=min(n, len(df)), random_state=args.seed).reset_index(drop=True)
        train = sub.iloc[: args.train_size]
        val = sub.iloc[args.train_size : args.train_size + args.val_size]

        os.makedirs(args.out_dir, exist_ok=True)
        train.to_parquet(os.path.join(args.out_dir, "train.parquet"), index=False)
        val.to_parquet(os.path.join(args.out_dir, "val.parquet"), index=False)
        print(f"wrote {len(train)} train / {len(val)} val -> {args.out_dir}")


    if __name__ == "__main__":
        main()

  run_async_longcot.sh: |
    #!/usr/bin/env bash
    set -xeuo pipefail

    # On ANY exit, kill background children (gpu monitor, watchdog, training)
    # so the stdout pipe to tee is released and the container surfaces the
    # real exit status.
    trap 'kill $(jobs -p) 2>/dev/null || true' EXIT

    RESULTS_DIR="/workspace/results"
    MODEL_PATH="/workspace/models/DeepSeek-R1-Distill-Qwen-1.5B"
    TRAIN_FILE="/workspace/data/dapo_math/train.parquet"
    VAL_FILE="/workspace/data/dapo_math/val.parquet"
    # RUN_SECS (not RUN_SECONDS) inside the script: keeps envsubst rendering
    # of the Job env from touching script-internal references.
    RUN_SECS="${RUN_SECONDS:-5400}"
    VERL_COMMIT="983cb0f24443f87b3d161fad318445130a620b07"

    mkdir -p "$RESULTS_DIR"

    echo "=== Phase 0: GPU access without the device plugin (hostPath driver mounts) ==="
    # Mirror the snapshot-agent DaemonSet: driver userspace at /usr/local/nvidia.
    export PATH="/usr/local/nvidia/bin:$PATH"
    export LD_LIBRARY_PATH="/usr/local/nvidia/lib64${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
    ldconfig /usr/local/nvidia/lib64 2>/dev/null || true

    # GPU_MASK is in NODE GPU indexes, SHARED GPU FIRST (e.g. "2,0").
    GPU_MASK="${GPU_MASK:?GPU_MASK env required, e.g. 2,0}"
    SHARED_NODE_IDX="${GPU_MASK%%,*}"
    DEDICATED_NODE_IDX="${GPU_MASK##*,}"

    NVIS=$(nvidia-smi -L | wc -l)
    echo "visible GPUs: $NVIS"
    nvidia-smi -L
    if [ "$NVIS" -ge 3 ]; then
        # Device plugin absent + privileged: all node GPUs visible, container
        # index == node index. CUDA_VISIBLE_DEVICES order is honored by CUDA,
        # so the shared GPU (first in mask) becomes cuda:0 -> Ray gives it to
        # the trainer placement group (created first).
        export CUDA_VISIBLE_DEVICES="$GPU_MASK"
        SHARED_UUID=$(nvidia-smi -i "$SHARED_NODE_IDX" --query-gpu=uuid --format=csv,noheader | tr -d ' ')
        DEDICATED_UUID=$(nvidia-smi -i "$DEDICATED_NODE_IDX" --query-gpu=uuid --format=csv,noheader | tr -d ' ')
    elif [ "$NVIS" = 2 ]; then
        # NVIDIA runtime honored NVIDIA_VISIBLE_DEVICES: only the 2 masked GPUs
        # are visible, renumbered. The shared GPU is node idx 2 = the HIGHER
        # PCI bus id of the pair (node index order == PCI order on a3-mega).
        mapfile -t BYBUS < <(nvidia-smi --query-gpu=index,pci.bus_id,uuid --format=csv,noheader | sort -t, -k2)
        DEDICATED_CIDX=$(echo "${BYBUS[0]}" | cut -d, -f1 | tr -d ' ')
        DEDICATED_UUID=$(echo "${BYBUS[0]}" | cut -d, -f3 | tr -d ' ')
        SHARED_CIDX=$(echo "${BYBUS[1]}" | cut -d, -f1 | tr -d ' ')
        SHARED_UUID=$(echo "${BYBUS[1]}" | cut -d, -f3 | tr -d ' ')
        export CUDA_VISIBLE_DEVICES="${SHARED_CIDX},${DEDICATED_CIDX}"
    else
        echo "FATAL: unexpected GPU visibility ($NVIS GPUs)"; exit 96
    fi
    echo "GPU mask resolved: shared=node${SHARED_NODE_IDX} uuid=$SHARED_UUID dedicated=node${DEDICATED_NODE_IDX} uuid=$DEDICATED_UUID CUDA_VISIBLE_DEVICES=$CUDA_VISIBLE_DEVICES"

    echo "=== Phase 1: install verl pinned @ $VERL_COMMIT ==="
    pip install datasets TransferQueue 2>&1 | tail -5
    git clone --depth 1 https://github.com/verl-project/verl.git /workspace/verl-src
    cd /workspace/verl-src
    git fetch --depth 1 origin "$VERL_COMMIT"
    git checkout "$VERL_COMMIT"
    git rev-parse HEAD | tee "$RESULTS_DIR/verl_commit"
    pip install -e ".[gpu]" 2>&1 | tail -10
    cd /workspace
    pip install cupy-cuda12x 2>&1 | tail -3
    python3 -c "import verl; print('verl version:', verl.__version__)"
    python3 -c "import transfer_queue; print('transfer_queue OK')"

    echo "=== Phase 1b: install timeslice lock client + verl plugin wrapper ==="
    pip install "git+https://github.com/llm-d-incubation/llm-d-rl-time-slicing.git#subdirectory=pkg/client/python" 2>&1 | tail -3
    # FIX 2026-07-26 (small-run finding): client generated stubs need
    # grpcio>=1.81 and protobuf gencode 7.35; image ships grpcio 1.80.0 /
    # protobuf 6.33.6. Upgrade both before the import check below.
    pip install "grpcio>=1.81.0" "protobuf>=7.35.0" 2>&1 | tail -2
    # Assemble the package tree from the ConfigMap mount (read-only) into a
    # writable dir; pyproject.toml is generated here (the overlay ships only
    # the module files — entry point per pkg-fully-async/INSTALL.md).
    mkdir -p /workspace/timeslice-verl-pkg/timeslice_verl
    cp -L /workspace/timeslice-pkg/__init__.py \
          /workspace/timeslice-pkg/fully_async.py \
          /workspace/timeslice-pkg/locks.py \
          /workspace/timeslice-verl-pkg/timeslice_verl/
    cat > /workspace/timeslice-verl-pkg/pyproject.toml <<'PYPROJ'
    [build-system]
    requires = ["setuptools>=61"]
    build-backend = "setuptools.build_meta"

    [project]
    name = "timeslice-verl"
    version = "0.3.0"
    description = "verl <-> llm-d-rl-time-slicing integration (PoC fully-async overlay)"
    requires-python = ">=3.9"

    [project.entry-points."verl.plugins"]
    timeslice_verl = "timeslice_verl"

    [tool.setuptools]
    packages = ["timeslice_verl"]
    PYPROJ
    pip install -e /workspace/timeslice-verl-pkg 2>&1 | tail -3
    python3 <<'CHK'
    import importlib.metadata as md
    eps = list(md.entry_points(group="verl.plugins"))
    assert any(ep.name == "timeslice_verl" for ep in eps), f"verl.plugins entry point missing: {eps}"
    import timeslice_verl  # noqa: F401  (fully-async hook installs; v1 trainers tolerated missing)
    try:
        from timeslice import OrchestratorClient  # noqa: F401
    except ImportError:  # renamed upstream to TimeSliceOrchestratorClient (small-run finding 2026-07-26; API identical)
        from timeslice import TimeSliceOrchestratorClient as OrchestratorClient
    print("timeslice wrapper install OK; verl.plugins entry points:", [ep.name for ep in eps])
    CHK

    echo "=== Phase 2: download model (DeepSeek-R1-Distill-Qwen-1.5B, MIT, ungated) ==="
    python3 -c "
    from huggingface_hub import snapshot_download
    snapshot_download('deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B', local_dir='$MODEL_PATH')
    print('Model download complete')
    "

    echo "=== Phase 3: prepare dataset (512 DAPO-Math-17k prompts) ==="
    python3 /workspace/scripts/data_prep.py --out_dir /workspace/data/dapo_math \
        --train_size 512 --val_size 64

    echo "=== Phase 3b: chat-template sanity check (forced <think> must be active) ==="
    python3 << 'TMPLCHECK'
    import pandas as pd
    from transformers import AutoTokenizer
    tok = AutoTokenizer.from_pretrained("/workspace/models/DeepSeek-R1-Distill-Qwen-1.5B")
    df = pd.read_parquet("/workspace/data/dapo_math/train.parquet")
    msgs = [dict(m) for m in df.iloc[0]["prompt"]]
    rendered = tok.apply_chat_template(msgs, add_generation_prompt=True, tokenize=False)
    print("RENDERED PROMPT TAIL:", rendered[-200:])
    assert rendered.rstrip().endswith("<think>"), "chat template does not force <think> - CoT premise broken"
    ntok = len(tok.apply_chat_template(msgs, add_generation_prompt=True))
    print(f"prompt tokens: {ntok}")
    assert ntok < 2048, f"prompt longer than max_prompt_length: {ntok}"
    print("Chat template check OK")
    TMPLCHECK

    echo "=== Phase 4: start in-pod GPU monitor (100ms; provenance only — the ==="
    echo "===          node-level async-poc-gpu-monitor pod is the canonical trace) ==="
    bash /workspace/scripts/gpu_monitor.sh "$RESULTS_DIR/gpu_util.csv" 100 &
    MONITOR_PID=$!
    date +%s%3N > "$RESULTS_DIR/start_ts"

    # ---------------------------------------------------------------------
    # ORIENTATION WATCHDOG: the run must DIE LOUDLY rather than freeze a
    # rollout. Every 15s: map compute pids -> GPU UUIDs, restrict to pids in
    # THIS pod's cgroup (hostPID shows the whole node), classify by cmdline
    # (WorkerDict = FSDP trainer, vllm/EngineCore = rollout), and assert:
    #   rollout pids NEVER on the shared GPU (node idx $SHARED_NODE_IDX),
    #   trainer pids NEVER on the dedicated GPU (node idx $DEDICATED_NODE_IDX).
    # On violation: ORIENTATION-FATAL + nvidia-smi dump + kill training.
    # ---------------------------------------------------------------------
    POD_CG_TOKEN=$(grep -o 'pod[0-9a-fA-F_-]\{36\}' /proc/self/cgroup | head -1 || true)

    pid_in_pod() {
        [ -n "$POD_CG_TOKEN" ] || return 0
        grep -q "$POD_CG_TOKEN" "/proc/$1/cgroup" 2>/dev/null
    }

    classify_pid() {
        local cmd
        cmd=$(tr '\0' ' ' < "/proc/$1/cmdline" 2>/dev/null || true)
        case "$cmd" in
            *WorkerDict*) echo trainer ;;
            *EngineCore*|*vllm*|*vLLM*|*VLLM*) echo rollout ;;  # FIX 2026-07-26: actual proc name is VLLM::Worker
            *) echo other ;;
        esac
    }

    kill_own_training() {
        # hostPID: NEVER pkill by pattern node-wide — restrict to our cgroup.
        local sig="$1" pat p
        for pat in fully_async_main "ray::" vllm python3; do
            for p in $(pgrep -f "$pat" 2>/dev/null || true); do
                [ "$p" = "$$" ] && continue
                pid_in_pod "$p" && kill "-$sig" "$p" 2>/dev/null || true
            done
        done
    }

    orientation_watchdog() {
        set +e
        local ok_logged=0 n=0 viol pid name uuid role seen_trainer seen_rollout
        while true; do
            sleep 15
            viol=0; seen_trainer=""; seen_rollout=""
            while IFS=, read -r pid name uuid; do
                pid=$(echo "$pid" | tr -d ' '); uuid=$(echo "$uuid" | tr -d ' ')
                [ -n "$pid" ] || continue
                pid_in_pod "$pid" || continue
                role=$(classify_pid "$pid")
                if [ "$role" = rollout ] && [ "$uuid" = "$SHARED_UUID" ]; then
                    viol=1; echo "ORIENTATION-VIOLATION vllm pid=$pid ($name) has compute context on SHARED GPU node${SHARED_NODE_IDX}"
                fi
                if [ "$role" = trainer ] && [ "$uuid" = "$DEDICATED_UUID" ]; then
                    viol=1; echo "ORIENTATION-VIOLATION trainer pid=$pid ($name) has compute context on DEDICATED GPU node${DEDICATED_NODE_IDX}"
                fi
                if [ "$role" = trainer ] && [ "$uuid" = "$SHARED_UUID" ]; then seen_trainer=$pid; fi
                if [ "$role" = rollout ] && [ "$uuid" = "$DEDICATED_UUID" ]; then seen_rollout=$pid; fi
            done < <(nvidia-smi --query-compute-apps=pid,process_name,gpu_uuid --format=csv,noheader 2>/dev/null)
            if [ "$viol" = 1 ]; then
                echo "ORIENTATION-FATAL: rollout/trainer GPU orientation violated — killing training (die loudly, never freeze a rollout)"
                nvidia-smi || true
                nvidia-smi --query-compute-apps=pid,process_name,gpu_uuid --format=csv || true
                touch "$RESULTS_DIR/ORIENTATION_FATAL"
                kill_own_training INT; sleep 10
                kill_own_training TERM; sleep 5
                kill_own_training KILL
                return 1
            fi
            if [ -n "$seen_trainer" ] && [ -n "$seen_rollout" ]; then
                if [ "$ok_logged" = 0 ] || [ $((n % 40)) -eq 0 ]; then
                    echo "ORIENTATION-OK trainer=GPU${SHARED_NODE_IDX} rollout=GPU${DEDICATED_NODE_IDX} (trainer_pid=$seen_trainer rollout_pid=$seen_rollout)"
                    ok_logged=1
                fi
            fi
            n=$((n + 1))
        done
    }

    echo "=== Phase 5: fully-async long-CoT training (time-sliced trainer) ==="
    echo "  Model: DeepSeek-R1-Distill-Qwen-1.5B | Dataset: DAPO-Math-17k (512 prompts)"
    echo "  lr=${ACTOR_LR:-1e-6} exp=${EXP_NAME:-unnamed} staleness=${STALENESS_THRESHOLD:-8}"
    echo "  timeslice: job=${TIMESLICE_JOB_ID:-?} group=${TIMESLICE_GROUP:-?} orch=${TIMESLICE_ORCH_ADDR:-?}"
    nvidia-smi

    export PYTHONUNBUFFERED=1
    export HYDRA_FULL_ERROR=1

    TOTAL_ROLLOUT_STEPS=65536   # >> trainer consumption; None-sentinel must not fire

    run_training() {
        # timeout bounds the experiment; exit 124 (timeout) is the EXPECTED end state.
        timeout --signal=INT --kill-after=120 "$RUN_SECS" \
        python3 -m verl.experimental.fully_async_policy.fully_async_main \
            data.train_files="$TRAIN_FILE" \
            data.val_files="$VAL_FILE" \
            data.prompt_key=prompt \
            data.truncation=left \
            data.max_prompt_length=2048 \
            data.max_response_length=16384 \
            data.train_batch_size=0 \
            data.gen_batch_size=1 \
            data.return_raw_chat=True \
            actor_rollout_ref.model.path="$MODEL_PATH" \
            actor_rollout_ref.model.use_remove_padding=True \
            actor_rollout_ref.model.enable_gradient_checkpointing=True \
            actor_rollout_ref.hybrid_engine=False \
            actor_rollout_ref.actor.strategy=fsdp \
            actor_rollout_ref.actor.ppo_mini_batch_size=64 \
            actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=1 \
            actor_rollout_ref.actor.use_kl_loss=False \
            actor_rollout_ref.actor.kl_loss_coef=0.0 \
            actor_rollout_ref.actor.entropy_coeff=0 \
            actor_rollout_ref.actor.grad_clip=1.0 \
            actor_rollout_ref.actor.loss_agg_mode=token-mean \
            actor_rollout_ref.actor.use_dynamic_bsz=True \
            actor_rollout_ref.actor.ppo_max_token_len_per_gpu=36864 \
            actor_rollout_ref.actor.optim.lr="${ACTOR_LR:-1e-6}" \
            actor_rollout_ref.actor.optim.lr_warmup_steps=-1 \
            actor_rollout_ref.actor.fsdp_config.param_offload=False \
            actor_rollout_ref.actor.fsdp_config.optimizer_offload=False \
            actor_rollout_ref.ref.log_prob_use_dynamic_bsz=True \
            actor_rollout_ref.ref.log_prob_max_token_len_per_gpu=36864 \
            actor_rollout_ref.rollout.name=vllm \
            actor_rollout_ref.rollout.mode=async \
            actor_rollout_ref.rollout.n=8 \
            actor_rollout_ref.rollout.calculate_log_probs=True \
            actor_rollout_ref.rollout.checkpoint_engine.backend=nccl \
            +actor_rollout_ref.rollout.checkpoint_engine.engine_kwargs.nccl.rebuild_group=True \
            actor_rollout_ref.rollout.gpu_memory_utilization=0.8 \
            actor_rollout_ref.rollout.tensor_model_parallel_size=1 \
            actor_rollout_ref.rollout.max_model_len=18432 \
            actor_rollout_ref.rollout.max_num_batched_tokens=18432 \
            actor_rollout_ref.rollout.enable_chunked_prefill=True \
            actor_rollout_ref.rollout.temperature=1.0 \
            actor_rollout_ref.rollout.top_p=1.0 \
            actor_rollout_ref.rollout.top_k=-1 \
            algorithm.adv_estimator=grpo \
            algorithm.use_kl_in_reward=False \
            algorithm.kl_ctrl.kl_coef=0.0 \
            trainer.logger="['console']" \
            trainer.val_before_train=False \
            trainer.save_freq=-1 \
            trainer.resume_mode=disable \
            trainer.nnodes=1 \
            trainer.n_gpus_per_node=1 \
            trainer.total_epochs=10 \
            trainer.test_freq=-1 \
            trainer.project_name=poc2-timeslice \
            trainer.experiment_name="${EXP_NAME:-async-poc}" \
            trainer.default_local_dir="$RESULTS_DIR/ckpts" \
            rollout.nnodes=1 \
            rollout.n_gpus_per_node=1 \
            rollout.total_rollout_steps="$TOTAL_ROLLOUT_STEPS" \
            async_training.staleness_threshold="${STALENESS_THRESHOLD:-8}" \
            async_training.trigger_parameter_sync_step=1 \
            async_training.use_dynamic_resource_scheduling=False \
            async_training.use_trainer_do_validate=False \
            2>&1 | tee "$RESULTS_DIR/train.log"
        return "${PIPESTATUS[0]}"
    }

    set +e
    run_training &
    TRAIN_PID=$!
    orientation_watchdog &
    WATCHDOG_PID=$!
    wait "$TRAIN_PID"
    TRAIN_RC=$?
    kill "$WATCHDOG_PID" 2>/dev/null || true
    set -e
    if [ -f "$RESULTS_DIR/ORIENTATION_FATAL" ]; then
        echo "RUN ABORTED: ORIENTATION-FATAL (see log above)"
        TRAIN_RC=97
    elif [ "$TRAIN_RC" = "124" ]; then
        echo "Training stopped by planned ${RUN_SECS}s timeout (expected end state)"
    elif [ "$TRAIN_RC" != "0" ]; then
        echo "WARNING: training exited rc=$TRAIN_RC before planned timeout"
    fi

    date +%s%3N > "$RESULTS_DIR/end_ts"
    kill "$MONITOR_PID" 2>/dev/null || true
    wait "$MONITOR_PID" 2>/dev/null || true
    wc -l "$RESULTS_DIR/gpu_util.csv" || true

    echo "=== Per-GPU summary (in-pod trace) ==="
    python3 -c "
    import csv
    from collections import defaultdict
    traces = defaultdict(list)
    with open('$RESULTS_DIR/gpu_util.csv') as f:
        for row in csv.DictReader(f):
            traces[int(row['gpu_index'])].append(float(row['gpu_util_pct']))
    for gi in sorted(traces.keys()):
        u = traces[gi]
        n = len(u)
        mean_u = sum(u)/n
        idle = sum(1 for v in u if v <= 15)/n
        busy = sum(1 for v in u if v >= 50)/n
        print(f'GPU {gi}: mean={mean_u:.1f}% idle={idle:.1%} busy={busy:.1%} samples={n}')
    " || true

    # Insurance: dump traces to stdout gzip+base64 so Cloud Logging preserves
    # them even if the live kubectl-cp window is missed (spot pool).
    echo "=== GPU trace dump (gzip+base64, prefix GPUCSV:) ==="
    echo "GPUCSV-BEGIN md5=$(md5sum "$RESULTS_DIR/gpu_util.csv" | cut -d' ' -f1)"
    gzip -c "$RESULTS_DIR/gpu_util.csv" | base64 -w 3000 | sed 's/^/GPUCSV:/' || true
    echo "GPUCSV-END"
    if [ -f "$RESULTS_DIR/rl_metrics.jsonl" ]; then
        echo "=== rl_metrics dump (gzip+base64, prefix RLMETRICS:) ==="
        echo "RLMETRICS-BEGIN md5=$(md5sum "$RESULTS_DIR/rl_metrics.jsonl" | cut -d' ' -f1)"
        gzip -c "$RESULTS_DIR/rl_metrics.jsonl" | base64 -w 3000 | sed 's/^/RLMETRICS:/' || true
        echo "RLMETRICS-END"
    else
        echo "WARNING: no rl_metrics.jsonl produced (wrapper inactive?)"
    fi

    echo "=== Keeping pod alive for result collection ==="
    sleep 7200
    if [ "$TRAIN_RC" = "97" ]; then exit 97; fi
---
apiVersion: batch/v1
kind: Job
metadata:
  name: poc2-job-a
  namespace: default
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 604800
  template:
    metadata:
      labels:
        app: poc2-job-a
        # Exact keys the platform selects on (pod-utils.go JobIDLabel/GroupLabel,
        # orchestrator kubernetes.go JobLabelKey/PodLabelKey):
        timeslice.io/job-id: job-a-trainer
        timeslice.io/group: trainers
    spec:
      restartPolicy: Never
      nodeName: ${NODE_NAME}
      hostPID: true                      # pidfd_getfd weight sync + agent pid discovery
      securityContext:
        seccompProfile:
          type: Unconfined               # pidfd_getfd blocked by default seccomp on GKE
      nodeSelector:
        cloud.google.com/gke-accelerator: nvidia-h100-mega-80gb
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: cloud.google.com/gke-nodepool
                operator: In
                values:
                - h100-mega-8gpu-spot-b
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
      containers:
      - name: benchmark
        image: verlai/verl:vllm020.dev2
        securityContext:
          privileged: true
          capabilities:
            add: ["SYS_PTRACE"]
        env:
        - name: VLLM_USE_V1
          value: "1"
        - name: STALENESS_THRESHOLD
          value: "8"
        - name: RUN_SECONDS
          value: "${RUN_SECONDS}"
        # Shared GPU FIRST in the mask (trainer PG is created first — findings Q7).
        # NVIDIA_VISIBLE_DEVICES is a no-op without the nvidia container runtime;
        # the run script derives CUDA_VISIBLE_DEVICES from GPU_MASK either way.
        - name: NVIDIA_VISIBLE_DEVICES
          value: "2,0"
        - name: GPU_MASK
          value: "2,0"
        - name: NVIDIA_DRIVER_CAPABILITIES
          value: "compute,utility"
        # FIX 2026-07-26 (nccl-restore-diagnosis.md): cuda-checkpoint does not
        # preserve cuMem/VMM allocations or cuMulticast (NVLS) objects; the
        # first post-restore comm creation died in transport/nvls.cc (cuda
        # error 101 -> NCCL_ERROR_UNHANDLED_CUDA_ERROR). Force cudaMalloc-based
        # transports. Set in ALL THREE manifests for config parity (baseline
        # data collected before 2026-07-26 ran WITHOUT these).
        - name: NCCL_CUMEM_ENABLE
          value: "0"
        - name: NCCL_NVLS_ENABLE
          value: "0"
        - name: ACTOR_LR
          value: "1e-6"
        - name: EXP_NAME
          value: "poc2-math-ts-a"
        # --- timeslice wrapper (timeslice_verl.fully_async) ---
        - name: TIMESLICE_FULLY_ASYNC
          value: "1"
        - name: TIMESLICE_JOB_ID
          value: "job-a-trainer"         # must equal the timeslice.io/job-id label
        - name: TIMESLICE_ORCH_ADDR
          value: "timeslice-acceleratororchestrator.timeslice-system.svc:50051"
        - name: TIMESLICE_GROUP
          value: "trainers"
        - name: TIMESLICE_METRICS_PATH
          value: "/workspace/results/rl_metrics.jsonl"
        command: ["/bin/bash", "-c"]
        args:
        - |
          set -euo pipefail
          echo "=== POC2 math-RLVR timeslice job-a (lr=1e-6, GPUs node2+node0) ==="
          echo "Node: $(hostname)"
          echo "Date: $(date)"
          bash /workspace/scripts/run_async_longcot.sh 2>&1 | tee /workspace/results/experiment.log
        resources:
          requests:
            cpu: "40"
            memory: "160Gi"
        volumeMounts:
        - name: scripts
          mountPath: /workspace/scripts
        - name: timeslice-pkg
          mountPath: /workspace/timeslice-pkg
        - name: results
          mountPath: /workspace/results
        - name: nvidia-driver
          mountPath: /usr/local/nvidia
          readOnly: true
        - name: nvidia-devices
          mountPath: /dev
        - name: dshm
          mountPath: /dev/shm
      volumes:
      - name: scripts
        configMap:
          name: poc2-job-a
          defaultMode: 0755
      - name: timeslice-pkg
        configMap:
          name: timeslice-pkg
          defaultMode: 0644
      - name: results
        emptyDir: {}
      - name: nvidia-driver
        hostPath:
          path: /home/kubernetes/bin/nvidia
          type: Directory
      - name: nvidia-devices
        hostPath:
          path: /dev
          type: Directory
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 64Gi
